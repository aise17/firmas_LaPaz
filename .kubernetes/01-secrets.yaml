# Opcional: solo si usas registry privado (ej. ghcr.io con imagen privada).
# Si la imagen es pública, no apliques este archivo y en 02-frontend-deployment.yaml
# no uses imagePullSecrets.
#
# Crear el secret manualmente:
#   kubectl create secret docker-registry docker-registry-secret \
#     --docker-server=ghcr.io \
#     --docker-username=TU_GITHUB_USER \
#     --docker-password=TU_GITHUB_TOKEN \
#     -n firmaslapaz
#
# Luego en 02-frontend-deployment.yaml descomenta imagePullSecrets.
---
# Placeholder: reemplaza .dockerconfigjson con el resultado de:
#   kubectl create secret docker-registry docker-registry-secret ... --dry-run=client -o yaml
apiVersion: v1
kind: Secret
metadata:
  name: docker-registry-secret
  namespace: firmaslapaz
type: kubernetes.io/dockerconfigjson
data:
  # Placeholder vacío (auths: {}). Sustituir por tu .dockerconfigjson en base64 si usas registry privado.
  .dockerconfigjson: eyJhdXRocyI6e319
